[pytest]
# Run from backend/ with: .venv/bin/pytest
#
# pythonpath = . puts backend/ on sys.path so tests can do `from app.main import app`.
# Without it pytest only adds tests/ to the path, and every import of app.* fails.
pythonpath = .
testpaths = tests
